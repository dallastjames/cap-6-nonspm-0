import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dallastjames.ionic.cap6.rc0.0.nonspm',
  appName: 'cap6-rc-0-non-spm',
  webDir: 'dist'
};

export default config;
